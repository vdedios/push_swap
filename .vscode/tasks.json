{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "cd ~/Desktop/42/push_swap && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "Close terminal",
            "type": "shell",
            "command": "killall -SIGTERM Terminal"
        }
    ]
}